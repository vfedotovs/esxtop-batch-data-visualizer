# Pinned for reproducible container builds.
# Resolved against python:3.11-slim. To refresh:
#   docker run --rm python:3.11-slim sh -c \
#     'pip install -q matplotlib flask werkzeug gunicorn && pip freeze'

# Direct dependencies
matplotlib==3.11.1
flask==3.1.3
werkzeug==3.1.8
gunicorn==26.0.0

# Transitive dependencies (pinned so builds are reproducible)
blinker==1.9.0
click==8.4.2
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
itsdangerous==2.2.0
Jinja2==3.1.6
kiwisolver==1.5.0
MarkupSafe==3.0.3
numpy==2.4.6
packaging==26.3
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
